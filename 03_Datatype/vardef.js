let way1 = "This is first way of declaring variable"
const way2 = "This is second way of declaring variable"

console.log(way1, way2);

// We dont use var for declaring variable because it conflict's on block level scope
